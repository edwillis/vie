coverage:
  status:
    project:
      default:
        target: 80%    # the required coverage value
        threshold: 2%  # the leniency in hitting the target
    patch:
      default:
        target: 80%  # Optional: Set a threshold for patch coverage
  # You can add more configuration options here if needed
